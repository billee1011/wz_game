package reader;

import java.io.File;

public class FilePath {
	public static final String PACK_CONFIG_PATH = "D:" + File.separator + "pathConfig.cfg";
	
	public static final String SERVER_CONFIG_PATH ="F:" + File.separator + "conf" +File.separator + "conf_file" + File.separator +"server" + File.separator;
	
	public static final String CLIENT_CONFIG_PATH = "F:" + File.separator + "conf" + File.separator+ "conf_file" +File.separator +  "client" + File.separator;
	
	public static final String DESIGN_CONF_PATH = "F:" +File.separator + "doc-design" + File.separator +"abc" +File.separator;
 }
